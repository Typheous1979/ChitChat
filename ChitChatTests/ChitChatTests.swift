import Testing

@Suite("ChitChat App Tests")
struct ChitChatTests {
    @Test("App launches successfully")
    func appLaunches() {
        // Integration tests will be added as features are built
        #expect(true)
    }
}
